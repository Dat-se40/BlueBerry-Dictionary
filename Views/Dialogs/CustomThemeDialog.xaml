<Window x:Class="BlueBerryDictionary.Views.Dialogs.CustomThemeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        Title="Customize Colors"
        Width="450"
        Height="520"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource CardBackground}"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True">

    <Border CornerRadius="20" Background="{DynamicResource CardBackground}"
            BorderBrush="{DynamicResource BorderColor}" BorderThickness="2">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Margin="0,0,0,20">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🎨" FontSize="28" Margin="0,0,12,0"/>
                    <TextBlock Text="Customize Colors"
                               FontSize="22"
                               FontWeight="Bold"
                               Foreground="{DynamicResource TextColor}"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <Button Content="×"
                        FontSize="36"
                        Width="45"
                        Height="45"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Background="Transparent"
                        BorderThickness="0"
                        Foreground="{DynamicResource TextColor}"
                        Cursor="Hand"
                        Click="Close_Click"/>
            </Grid>

            <!-- Color Pickers -->
            <StackPanel Grid.Row="1">
                <!-- Primary Color -->
                <TextBlock Text="Primary Color:"
                           FontWeight="SemiBold"
                           FontSize="15"
                           Margin="0,0,0,10"
                           Foreground="{DynamicResource TextColor}"/>
                <xctk:ColorPicker x:Name="PrimaryColorPicker"
                                  SelectedColor="#2D4ACC"
                                  Height="45"
                                  Margin="0,0,0,20"
                                  ShowStandardColors="True"
                                  ShowRecentColors="True"
                                  UsingAlphaChannel="False"/>

                <!-- Secondary Color -->
                <TextBlock Text="Secondary Color:"
                           FontWeight="SemiBold"
                           FontSize="15"
                           Margin="0,0,0,10"
                           Foreground="{DynamicResource TextColor}"/>
                <xctk:ColorPicker x:Name="SecondaryColorPicker"
                                  SelectedColor="#5B7FFF"
                                  Height="45"
                                  Margin="0,0,0,20"
                                  ShowStandardColors="True"
                                  ShowRecentColors="True"
                                  UsingAlphaChannel="False"/>

                <!-- Accent Color -->
                <TextBlock Text="Accent Color:"
                           FontWeight="SemiBold"
                           FontSize="15"
                           Margin="0,0,0,10"
                           Foreground="{DynamicResource TextColor}"/>
                <xctk:ColorPicker x:Name="AccentColorPicker"
                                  SelectedColor="#1A2D6D"
                                  Height="45"
                                  Margin="0,0,0,20"
                                  ShowStandardColors="True"
                                  ShowRecentColors="True"
                                  UsingAlphaChannel="False"/>
            </StackPanel>

            <!-- Apply Button -->
            <Button Grid.Row="2"
                    Content="✓ Apply"
                    Style="{StaticResource SecondaryButtonStyle}"
                    Padding="0,14"
                    FontSize="16"
                    Click="Apply_Click"/>
        </Grid>
    </Border>
</Window>
